# 🏆 وثيقة توثيق إنجازات مشروع: منصة إتقان بايثون (Python Mastery Platform) 🐍

> ⚡ **"كود متقن، منصة ذكية، وتجربة استثنائية.. وده يدلّ إنّ بشمهندس يوسف، بشمهندس مش عادي" 🚀👑**

---

## 📌 نبذة عامة عن المشروع
منصة تعليمية تفاعلية متكاملة ومتوافقة مع معايير الـ Production لتعليم لغة بايثون، مبنية على أسلوب **فتح الدروس المشروط (Gated Progression)**، وتوفير **مهام مباشرة وتحديات مراجعة تراكمية شاملة** مع محرر كود وتصحيح واختبار آلي فوري، مصممة بنظام أمان وحسابات مستقلة لكل طالب وقابلة للتوسع السحابي.

---

## 🗺️ ما تم إنجازه تفصيلاً خطوة بخطوة (Changelog & Phases)

### ✅ المرحلة 1: بناء المنصة الأساسية وديناميكية المحتوى (Phase 1)
1. **نظام فتح الدروس التدريجي (Gated Progression)**:
   - الدرس التالي يظل **مقفلاً 🔒** تماماً ولا يفتح إلا إذا اجتاز الطالب جميع مهام ومراجعات الدرس السابق.
2. **مهام الدرس المباشرة (Direct Tasks)**:
   - تطبيق فوري على المفهوم المشروح (تمرينان لكل درس).
3. **مهام المراجعة التراكمية (Cumulative Review Tasks)**:
   - تحديات برمجية ذكية تربط ما درسه الطالب في جميع الدروس السابقة بالدرس الحالي لضمان عدم نسيان الأساسيات.
4. **ديناميكية المحتوى (Dynamic Material)**:
   - الدروس موجودة في مجلد `lessons/` كملفات `JSON` مستقلة.
   - يمكن إضافة أو تعديل أي درس في ثوانٍ دون لمس كود المنصة، وتقوم المنصة بقراءتها وترتيبها تلقائياً.
5. **جاهزية الاستضافة السحابية**:
   - قراءة المنفذ ديناميكياً `PORT = int(os.environ.get("PORT", 8080))` للعمل على Render و Heroku ومحلياً بسلاسة.
   - إزالة أي مكتبات غير مستخدمة من `requirements.txt`.

---

### ✅ المرحلة 2: محرك عزل الأمان الأساسي للكود (Phase 2 - Basic Sandbox)
1. **فصل محرك التشغيل في ملف مستقل (`runner.py`)**:
   - فصل سيرفر الويب عن بيئة تشغيل أكواد الطلاب.
2. **جدار حماية الكود الثابت (AST Static Analysis Guard)**:
   - حظر استيراد موديولات النظام الحساسة: `os`, `sys`, `subprocess`, `shutil`, `socket`, `urllib`, `requests`, `ctypes`, إلخ.
   - حظر الدوال الخطيرة: `open()`, `eval()`, `exec()`, `__import__()`, `compile()`, `globals()`, إلخ.
   - حظر هجمات الهروب البرمجي (Sandbox Escape via Dunder methods) مثل: `__subclasses__()`, `__bases__`, `__mro__`.
3. **حماية الموارد (Resource Limiting)**:
   - حماية من اللوب اللانهائي (`timeout=4s`).
   - تحديد الحد الأقصى لحجم الكود (`MAX_CODE_LENGTH = 10,000` حرف).
   - تشغيل العملية بعلم العزل `-I` و `-B` لمنع توريث أي بيئة أو صلاحيات من السيرفر.

---

### ✅ المرحلة 2.5: معمارية حاويات الساندبوكس للإنتاج (Phase 2.5 - Production Sandbox)
1. **حاوية الساندبوكس (`sandbox/Dockerfile`)**:
   - بيئة Python خفيفة بدون أي صلاحيات (`non-root user`: `sandboxuser`).
   - إسقاط جميع صلاحيات النظام (`--cap-drop ALL`).
   - منع رفع الصلاحيات (`--security-opt no-new-privileges`).
   - عزل الشبكة بالكامل (`--network none`).
   - حدود الموارد:
     - الذاكرة (`--memory 128m --memory-swap 128m`).
     - المعالج (`--cpus 0.5`).
     - العمليات (`--pids-limit 16`) لمنع هجمات Fork Bombs.
     - نظام ملفات للقراءة فقط (`--read-only`) مع `tmpfs` مقيد ومؤقت فقط.
2. **خدمة تشغيل مستقلة (`runner_service/server.py` و `runner_service/Dockerfile`)**:
   - خدمة ميكروسيرفيس تتولى تشغيل الكود عبر API محمي بمفتاح سري مشفر (`X-Runner-Secret`).
3. **ملف `docker-compose.yml`**:
   - يربط تطبيق الويب بخدمة الساندبوكس تلقائياً.
4. **نظام المعمارية الثنائية (Dual Mode) في `runner.py`**:
   - نمط الإنتاج: يرسل الكود للحاوية المعزولة تلقائياً عند توفر `RUNNER_URL`.
   - نمط التطوير المحلي: يعمل بالعزل المحلي والـ AST Guard في حال غياب Docker.

---

### ✅ المرحلة 3: نظام المستخدمين والتقدم المنفصل (Phase 3 - Users & Auth)
1. **تسجيل الدخول والمصادقة الآمنة في `db.py`**:
   - جدول المستخدمين `users` مع تجزئة كلمات المرور باستخدام المعيار العالمي `PBKDF2-HMAC-SHA256` مع Salt عشوائي فريد لكل مستخدم.
   - جدول الجلسات `sessions` لرموز الدخول الآمنة المشفرة.
2. **فصل تقدم الطلاب بنسبة 100%**:
   - جدول `completed_tasks` وجدول `completed_lessons` مرتبطان بـ `user_id`.
   - إنجاز أي طالب لدرس أو تمرين لا يؤثر على باقي الطلاب، ولكل طالب حسابه ومساره المستقل.
3. **واجهة تفاعلية كاملة (Authentication UI)**:
   - نافذة منبثقة (Modal) لتسجيل الدخول وإنشاء حساب جديد بسلاسة.
   - شارة في شريط العنوان تعرض اسم الطالب وزر لتسجيل الخروج.
   - حفظ الجلسات عبر `HttpOnly, SameSite=Lax` Cookies.

---

### ✅ المرحلة 4: نقل وتجهيز طبقة PostgreSQL والـ Migrations (Phase 4)
1. **نظام الـ Migrations الموثق (`migrations/001_initial_schema.sql`)**:
   - جداول `users`, `sessions`, `lessons`, `tasks`, `completed_tasks`, `completed_lessons`.
   - مؤشرات أداء (Indexes) لتسريع البحث والاستعلام مع آلاف الطلاب.
   - مفاتيح أساسية مركبة وعلاقات `ON DELETE CASCADE`.
2. **طبقة الاتصال الشاملة (`db.py`)**:
   - دعم **Connection Pooling** (`psycopg2.pool.SimpleConnectionPool`) لإدارة الاتصالات بكفاءة عالية ومنع استنزاف السيرفر.
   - دعم كامل لمتغير البيئة `DATABASE_URL` (متوافق مع Render, Railway, Supabase, Neon).
   - التحويل التلقائي لروابط `postgres://` القديمة إلى `postgresql://`.
   - تراجع تلقائي سلس (Seamless Fallback) إلى SQLite محلياً أثناء التطوير بدون إنترنت.
3. **إدارة العمليات (Transactions)**:
   - دعم كامل لـ `Context Managers` مع `commit()` و `rollback()` تلقائي عند حدوث أي خطأ.

---

## 🧪 حزم الاختبارات البرمجية وضمان الجودة (Test Suites)
تم إنشاء 3 ملفات اختبارات تغطي كافة زوايا النظام:
1. **`test_sandbox.py`**: يختبر 7 سيناريوهات أمنية (حظر `os`, `subprocess`, `open()`, `eval()`, الدوال الخاصة `__subclasses__`, واللوب اللانهائي).
2. **`test_users.py`**: يختبر إنشاء الحسابات، تسجيل الدخول، الجلسات، واستقلالية تقدم كل مستخدم عن الآخر.
3. **`test_platform.py`**: يختبر تحميل الدروس، شروط القفل التدريجي، ومحرك التقييم والتصحيح.

> 🟢 **النتيجة**: اجتياز جميع الاختبارات بنسبة نجاح **100%**!

---

## 📁 الهيكل الحالي لملفات المشروع (Directory Structure)
```text
learning-a-python-by-a-websit/
├── app.py                     # سيرفر المنصة الرئيسي والـ APIs
├── engine.py                  # محرك احتساب التقدم والتقييم
├── db.py                      # طبقة قاعدة البيانات الشاملة (PostgreSQL / SQLite)
├── runner.py                  # عميل تشغيل الأكواد الآمن ومراقب الـ AST
├── requirements.txt           # الحزم المطلوبة للإنتاج (psycopg2-binary)
├── Dockerfile                 # صورة الـ Container لتطبيق الويب
├── docker-compose.yml         # تشغيل تطبيق الويب مع خدمة الساندبوكس
├── README.md                  # توثيق المشروع الرسمي
├── HOW_TO_ADD_LESSONS.md      # دليل إضافة الدروس والمهام مستقبلاً
├── PROJECT_OVERVIEW.md        # هذه الوثيقة الشاملة لكل ما تم إنجازه
│
├── migrations/                # مجلد الـ SQL Migrations
│   └── 001_initial_schema.sql # هيكل الـ Database الاحترافي للإنتاج
│
├── runner_service/            # خدمة الساندبوكس المصغرة (Microservice)
│   ├── Dockerfile
│   └── server.py
│
├── sandbox/                   # الحاوية المعزولة لتنفيذ كود الطالب
│   └── Dockerfile
│
├── lessons/                   # الدروس التفاعلية الديناميكية
│   ├── 01_intro_print.json
│   ├── 02_variables.json
│   └── 03_conditionals.json
│
├── static/css/                # ملفات التنسيق المحلية المستقلة
│   └── style.css
│
├── templates/                 # الواجهة الأمامية للمنصة
│   └── index.html
│
└── الاختبارات (Tests)/
    ├── test_platform.py
    ├── test_sandbox.py
    └── test_users.py
```

---

## 🔗 حالة المستودع على GitHub:
- **المستودع**: [youssef-elsafty/platform-python](https://github.com/youssef-elsafty/platform-python)
- **الفرع**: `main`
- **الحالة**: جميع الملفات والتحديثات أعلاه تم عمل Commit و Push لها بنجاح وأحدث نسخة مرفوعة بالكامل.
